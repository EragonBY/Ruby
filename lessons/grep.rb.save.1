#!/usr/bin/ruby

def grep (*arg)
for current<max do
 case arg[current]
    when "-A"
      current+=
      puts "option = -A"
    when "-R"
      puts "option = -R"
    when "-Z"
      puts "option = -Z"
    when "-G"
      puts "option = -G"     
  end
  current+=
end
end
puts ARGV.
grep(ARGV[0])
